apiVersion: v1
kind: Service
metadata:
  name: {{ include "crud-helm.fullname" . }}
  labels:
    {{- include "crud-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crud-helm.selectorLabels" . | nindent 4 }}
