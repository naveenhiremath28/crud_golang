apiVersion: kustomize.config.k8s.io/v1beta1 
kind: Kustomization

namespace: crud-golang

resources: 
  - name-space.yaml 
  - config-map.yaml 
  - secrets.yaml 
  - postgres-deployment.yaml 
  - keycloak.yaml 
  - deployment.yaml 
  - ingress.yaml
  
configMapGenerator: 
  - name: keycloak-realm 
    files: 
      - realm-export.json